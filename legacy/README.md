# Legacy: Python API version

The first build: a FastAPI backend (29 passing tests) with n8n in front. Superseded on 2026-09-21 by the Google-Sheets-as-database version in `n8n/`, where all logic runs inside n8n. Kept as a tested reference for the rules (risk, verification, secret guard).
